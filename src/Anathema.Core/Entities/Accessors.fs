namespace Anathema.Core.Entities

//open Anathema.Core.Entities
//open Anathema.Core.Components

//module Accessors =
    //module Entity =
        //let inline agency (entity: Entity) = entity.Get<Agency>(Component.Agency)
        //let inline position (entity: Entity) = entity.Get<Position>(Component.Position)
        //let inline visibility (entity: Entity) = entity.Get<Visibility>(Component.Visibility)

    //type Entity with
        //member this.Agency = Entity.agency this
        //member this.Position = Entity.position this
        //member this.Visibility = Entity.visibility this

//module Updaters =
    //module Entity =
        //let inline setPosition (entity: Entity) (p: Position) =
            //entity.With(Component.Position, p)

    //type Entity with
        //member this.WithPosition p = Entity.setPosition this p


    